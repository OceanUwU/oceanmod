{
    "modid": "${project.artifactId}",
    "name": "${project.name}",
    "author_list": ["ocean", "cuboid consumer"],
    "description": "${project.description}",
    "version": "${project.version}",
    "dependencies": ["basemod"]
}