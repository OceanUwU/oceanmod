{
    "modid": "${project.artifactId}",
    "name": "${project.name}",
    "author_list": ["ocean"],
    "description": "${project.description}",
    "version": "${project.version}",
    "dependencies": ["basemod"]
}